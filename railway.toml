[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./start.sh"
healthCheckPath = "/api/health"
healthCheckTimeout = 300

[[services]]
name = "web"
port = 8000

[services.env]
NODE_ENV = "production"